# skhd configuration - converted from AeroSpace config
# Leader key: alt (Option)
# Using vim-style hjkl for navigation

# ====== Focus windows (vim keys) ======
alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - l : yabai -m window --focus east

# ====== Move windows (vim keys with Shift) ======
alt + shift - h : yabai -m window --swap west
alt + shift - j : yabai -m window --swap south
alt + shift - k : yabai -m window --swap north
alt + shift - l : yabai -m window --swap east

# ====== Switch between spaces/workspaces ======
alt - 1 : yabai -m space --focus 1
alt - 2 : yabai -m space --focus 2
alt - 3 : yabai -m space --focus 3
alt - 4 : yabai -m space --focus 4
alt - 5 : yabai -m space --focus 5
alt - 6 : yabai -m space --focus 6
alt - 7 : yabai -m space --focus 7
alt - 8 : yabai -m space --focus 8
alt - 9 : yabai -m space --focus 9

# ====== Move window to workspace ======
# Note: Without disabling SIP, we use a workaround that simulates macOS native shortcuts
# This moves the window and follows it to the new space
alt + shift - 1 : yabai -m window --space 1; yabai -m space --focus 1
alt + shift - 2 : yabai -m window --space 2; yabai -m space --focus 2
alt + shift - 3 : yabai -m window --space 3; yabai -m space --focus 3
alt + shift - 4 : yabai -m window --space 4; yabai -m space --focus 4
alt + shift - 5 : yabai -m window --space 5; yabai -m space --focus 5
alt + shift - 6 : yabai -m window --space 6; yabai -m space --focus 6
alt + shift - 7 : yabai -m window --space 7; yabai -m space --focus 7
alt + shift - 8 : yabai -m window --space 8; yabai -m space --focus 8
alt + shift - 9 : yabai -m window --space 9; yabai -m space --focus 9

# ====== Layout controls ======
# Toggle split orientation (horizontal/vertical)
alt - s : yabai -m window --toggle split

# Fullscreen toggle
alt - f : yabai -m window --toggle zoom-fullscreen

# Float toggle
alt + shift - space : yabai -m window --toggle float --grid 4:4:1:1:2:2

# Balance layout (equalize window sizes)
alt + shift - 0x18 : yabai -m space --balance  # 0x18 is the keycode for '='

# ====== Stack/Accordion layouts ======
# Vertical stack
alt + shift - s : yabai -m window --toggle zoom-parent

# Horizontal stack
alt + shift - w : yabai -m window --toggle zoom-parent

# ====== Window management ======
# Close window (uses macOS native close)
alt + shift - q : yabai -m window --close

# Reload yabai configuration
alt + shift - c : launchctl kickstart -k "gui/${UID}/homebrew.mxcl.yabai"

# ====== Resize mode ======
# Enter resize mode with alt-r, then use hjkl to resize
# Exit with escape or enter
resize < h : yabai -m window --resize left:-50:0 || yabai -m window --resize right:-50:0
resize < j : yabai -m window --resize bottom:0:50 || yabai -m window --resize top:0:50
resize < k : yabai -m window --resize bottom:0:-50 || yabai -m window --resize top:0:-50
resize < l : yabai -m window --resize right:50:0 || yabai -m window --resize left:50:0
resize < 0x18 : yabai -m space --balance  # '=' key
resize < escape : skhd -k "cmd + alt + ctrl - r"  # Exit resize mode
resize < return : skhd -k "cmd + alt + ctrl - r"  # Exit resize mode

# Toggle resize mode
alt - r : skhd -k "cmd + alt + ctrl - r"
cmd + alt + ctrl - r : skhd -m "resize"

# ====== Monitor/Display focus ======
# Focus different displays (if you have multiple monitors)
alt + shift - 1 : yabai -m display --focus 1
alt + shift - 2 : yabai -m display --focus 2
alt + shift - 3 : yabai -m display --focus 3

# ====== Fast workspace switching (like alt-tab) ======
# Switch to recent workspace
alt - tab : yabai -m space --focus recent

# ====== Rotate windows ======
alt - r : yabai -m space --rotate 90

# ====== Mirror windows ======
alt - y : yabai -m space --mirror y-axis
alt - x : yabai -m space --mirror x-axis
